# ── Python ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg
*.egg-info/
build/
*.so
.Python
env/
venv/
.venv/
ENV/
*.env

# ── Jupyter ───────────────────────────────────────────────────
.ipynb_checkpoints/

# ── ML Model binaries ─────────────────────────────────────────
# Note: models/*.pkl are large binaries regenerated by the ML pipeline.
# Uncomment the line below to EXCLUDE them from git (saves repo space).
# To use the project, run the ML pipeline to regenerate them locally.
# models/*.pkl

# ── Data ──────────────────────────────────────────────────────
# Raw data folder (empty in this project — placeholder only)
data/raw/

# Generated predictions and processed data ARE committed intentionally
# so the backend can start without running the ML pipeline:
# data/predictions/ — intentionally tracked
# data/processed/   — intentionally tracked

# ── Node / Frontend ───────────────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend/.env
frontend/.env.local

# ── OS ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDEs ──────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── Logs ──────────────────────────────────────────────────────
*.log
logs/

# ── ResQ Shield real research datasets ───────────────────────
data_real/**/*.parquet
data_real/**/*.tif
data_real/**/*.csv
data_real/**/*.geojson
data_real/**/*.json
data_real/admin/raw/
data_real/terrain/raw/
models_real/

# ── Local/debug artifacts ─────────────────────────────────────
temp_api.json
append_route.py
compute_stats.py
test_evacuation_endpoint.py
test_mandi.py
test_overpass.py
